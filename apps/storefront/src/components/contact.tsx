'use client';
import { useState } from 'react';
import { post,errorMessage } from '@/lib/api';
import { ArrowUpRight } from './icons';
export function ContactForm(){const [message,setMessage]=useState(''),[pending,setPending]=useState(false);return <form className="form-panel" onSubmit={async e=>{e.preventDefault();const form=e.currentTarget;setPending(true);try{await post('/contact',Object.fromEntries(new FormData(form)));setMessage('Message received. Our team will be in touch.');form.reset();}catch(error){setMessage(errorMessage(error));}finally{setPending(false);}}}><div className="form-grid"><label>Your name<input name="name" required minLength={2} maxLength={100} autoComplete="name"/></label><label>Email address<input name="email" type="email" required autoComplete="email"/></label><label>Phone (optional)<input name="phone" type="tel" autoComplete="tel"/></label><label>Order number (optional)<input name="orderNumber"/></label></div><label>Subject<input name="subject" minLength={3} maxLength={200} required/></label><label>Your message<textarea name="message" required minLength={10} maxLength={5000}/></label><button className="button" disabled={pending}>{pending?'Sending…':'Send message'}<ArrowUpRight size={17}/></button><p role="status" className="form-message">{message}</p></form>;}
